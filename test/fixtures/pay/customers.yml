one:
  owner: subscribed_business (User)
  processor: stripe
  processor_id: cus_1234
  default: true

# two:
#   owner: business (User)
#   processor: stripe
#   processor_id: cus_12345
#   default: true
