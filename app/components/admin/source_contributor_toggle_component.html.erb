<% if developer.source_contributor? %>
  <%= render Admin::BadgeComponent.new(t(".active")) %>
  <%= render Admin::Forms::ButtonLinkComponent.new(t(".deactivate"), form: "deactivate-form-#{developer.id}") %>
<% else %>
  <%= render Admin::Forms::ButtonLinkComponent.new(t(".activate"), form: "activate-form-#{developer.id}") %>
<% end %>

<%= form_with url: admin_developer_source_contributors_path(developer), method: :create, id: "activate-form-#{developer.id}" do %>
<% end %>
<%= form_with url: admin_developer_source_contributors_path(developer), method: :delete, id: "deactivate-form-#{developer.id}" do %>
<% end %>
