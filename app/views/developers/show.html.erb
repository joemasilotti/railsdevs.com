<% content_for :open_graph_tags do %>
  <%= render DeveloperOpenGraphTagsComponent.new(developer: @developer) %>
<% end %>

<div class="mt-0 lg:mt-16 pb-8 mb-16 bg-white shadow lg:rounded-lg overflow-hidden">
  <%= render CoverImageComponent.new(developer: @developer, classes: "lg:rounded-t-lg") %>

  <div class="relative z-10 max-w-5xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="-mt-12 sm:-mt-16 sm:flex sm:items-end sm:space-x-5">
      <%= render AvatarComponent.new(developer: @developer) %>
    </div>
  </div>

<<<<<<< HEAD
  <div class="flex-1">
    <div class="pt-6 pb-8 xl:pb-10">
      <div class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8 lg:max-w-5xl lg:grid lg:grid-cols-3">
        <div class="lg:col-span-2 lg:pr-8 lg:border-r lg:border-gray-200">
          <div>
            <div>
              <div class="md:flex md:items-center md:justify-between md:space-x-4 lg:border-b lg:pb-6">
                <h1 class="text-2xl font-bold text-gray-900"><%= @developer.hero %></h1>
              </div>

              <aside class="mt-8 lg:hidden">
                <%= render "details", developer: @developer %>
              </aside>

              <div class="py-3 lg:pt-6 lg:pb-0">
                <h2 class="sr-only">Description</h2>
                <div class="prose max-w-prose">
                  <%= simple_format @developer.bio %>
                </div>
              </div>
            </div>
          </div>
=======
  <div class="mt-8 max-w-5xl mx-auto px-4 sm:px-6 lg:px-8">
    <dl class="max-w-prose grid grid-cols-1 gap-x-4 gap-y-8 sm:grid-cols-2">
      <div class="sm:col-span-1">
        <dt class="text-sm font-medium text-gray-500">
          Availability
        </dt>
        <dd class="flex mt-1 text-sm text-gray-900">
          <%= render AvailabilityComponent.new(developer: @developer) %>
        </dd>
      </div>

      <% if @developer.expected_hourly_rate.present? %>
        <div class="sm:col-span-1">
          <dt class="text-sm font-medium text-gray-500">
            Hourly rate
          </dt>
          <dd class="flex mt-1 text-sm text-gray-900">
            <%= number_to_currency @developer.expected_hourly_rate %>
          </dd>
        </div>
      <% end %>

      <% if @developer.expected_salary.present? %>
        <div class="sm:col-span-1">
          <dt class="text-sm font-medium text-gray-500">
            Yearly salary
          </dt>
          <dd class="flex mt-1 text-sm text-gray-900">
            <%= number_to_currency @developer.expected_salary %>
          </dd>
        </div>
      <% end %>

      <% if @developer.website.present? %>
        <div class="sm:col-span-1">
          <dt class="text-sm font-medium text-gray-500">
            Website
          </dt>
          <dd class="mt-1 text-sm text-gray-900">
            <%= render ExternalLinkComponent.new(@developer.website, @developer.website, class: "hover:text-gray-600") %>
          </dd>
>>>>>>> 54bd724 (Show salary and hourly rate if present.)
        </div>

        <aside class="hidden lg:block lg:pl-8">
          <%= render "details", developer: @developer %>
        </aside>
      </div>
    </div>
  </div>
</div>
