<p><%= link_to "#{@business.name} (#{@business.company})", business_url(@business) %> started a conversation with <%= link_to @developer.name, notification_url(@notification) %>:</p>

<%== html_truncate_message_body(@body) %>

<hr>

<% @subscriptions.each do |subscription| %>
  <ul>
    <li><b>Plan</b>: <%= BusinessSubscription.from(subscription.processor_plan).name %></li>
    <li><b>Status</b>: <%= subscription.status.humanize %></li>
    <li><b>Developer roles</b>: <%= @developer.role_type.humanize %></li>
  </ul>
<% end %>
