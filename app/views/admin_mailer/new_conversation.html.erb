<p><%= link_to "#{@business.contact_name} (#{@business.company})", business_url(@business) %> started a conversation with <%= link_to @developer.name, developer_url(@developer) %>:</p>

<%== html_truncate_message_body(@body) %>

<%= link_to "View conversation", notification_url(@notification) %>

<hr>

<% @subscriptions.each do |subscription| %>
  <ul>
    <li><b>Plan</b>: <%= Businesses::Subscription.with_price_id(subscription.processor_plan).name %></li>
    <li><b>Status</b>: <%= subscription.status.humanize %></li>
    <li><b>Developer roles</b>: <%= @developer.role_type.humanize %></li>
  </ul>
<% end %>
