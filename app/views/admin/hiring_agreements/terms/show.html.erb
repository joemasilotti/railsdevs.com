<%= render Admin::ContainerComponent.new do %>
  <%= render Admin::ContentComponent.new(width: "max-w-3xl") do %>
    <%= render Admin::DescriptionListComponent.new(t(".title"), @term.created_at.strftime("%b %d, %Y")) do |list| %>
      <% list.with_link "Edit", edit_admin_hiring_agreements_term_path(@term) %>
      <% list.with_description t(".body"), simple_format(@term.body), wide: true %>
    <% end %>
  <% end %>
<% end %>
