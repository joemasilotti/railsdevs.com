<% I18n.available_locales.each do |locale| %>
  <link rel="alternate" href="<%= request.base_url + url_for(locale: resolve_locale(locale)) %>" hreflang="<%= locale == I18n.locale ? "x-default" : locale %>">
<% end %>
