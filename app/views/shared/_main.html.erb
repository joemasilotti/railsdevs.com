<%= render NewDeveloperFieldsComponent.new(current_account, enabled: Feature.enabled?(:new_developer_fields_banner, user: current_user)) %>
<%= render "shared/flash" %>

<main class="mx-auto">
  <%= yield %>
</main>
